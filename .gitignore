# Секреты и личные привязки — в репозиторий не идут
.env
.env.*
!.env.example
.clasp.json
.deploy.json
.vercel/
node_modules/

# Личные данные семьи: выписки, номера карт и счетов.
# Репозиторий публичный, а выкатка делает `git add -A` — без этой строки
# всё, что сюда положено, уедет на GitHub.
.private/
*.csv
*.xlsx

# Временные файлы проверок
tests/_sandbox.js
*.log
