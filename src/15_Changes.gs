/**
 * 15_Changes.gs — что нового в этой версии.
 *
 * Файл собирается автоматически из CHANGELOG.md (node tools/build.js).
 * Править руками не нужно: при следующей сборке всё перезапишется.
 *
 * Список лежит в коде, а не берётся с GitHub, потому что бот рассказывает
 * о нём сразу после обновления — когда репозиторий ещё отдаёт из кэша
 * прежнюю версию, а сети может не быть вовсе.
 */

var BOT_VERSION_DATE = "22.08.2026";

var BOT_CHANGES = [
  "Правка справочника теперь сильнее автообучения. Разобрав магазин, бот дописывает его полное название в ключевые слова — и это название длиннее любого ручного ключа, а выигрывает самое длинное совпадение. Из-за этого «курс у Светланы Зингер» упорно оставался «уходом за собой», сколько бы ключевых слов туда ни добавляли",
  "Теперь при правке справочника конфликтующие ключи из чужих категорий убираются, а бот сообщает, сколько их было"
];
